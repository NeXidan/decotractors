let webpack = require('webpack');

module.exports = {
    entry: './src/decotractors.js',
    output: {
        path: __dirname + '/dist',
        filename: 'decotractors.min.js',
        libraryTarget: 'umd'
    },
    externals: ['lodash'],
    module: {
        loaders: [
            {
                test: /\.js/,
                exclude: /node_modules/,
                loader: 'babel-loader'
            }
        ]
    },
    plugins: [
        new webpack.optimize.UglifyJsPlugin({minimize: true})
    ]
};
